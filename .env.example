# Configurações SMTP para envio de email
# Host do servidor SMTP (ex: smtp.gmail.com, smtp.hostinger.com, smtp-mail.outlook.com)
SMTP_HOST=

# Porta SMTP (587 para TLS, 465 para SSL)
SMTP_PORT=

# Usar TLS/STARTTLS (true) ou SSL (false)
SMTP_SECURE=

# Usuário SMTP (geralmente seu email)
SMTP_USER=

# Senha SMTP ou App Password (para Gmail use App Password)
# IMPORTANTE: Remova TODOS os espaços da senha de app!
# Exemplo: se o Gmail mostrar "abcd efgh ijkl mnop", use "abcdefghijklmnop"
SMTP_PASS=

# Email que enviará os formulários (geralmente o mesmo que SMTP_USER)
EMAIL_FROM=

# Email que receberá os contatos do formulário (pode ser qualquer email)
EMAIL_TO=

# URL do site (produção)
SITE_URL=
